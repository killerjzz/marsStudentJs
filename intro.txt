// 结论
1 闭包是为了防止变量污染
2 模块化

3 js中函数是第一公民
js中只有函数才能建立变量作用域

闭包是通过函数中套函数来实现的

// getId
// 每次调用它 都会让返回值+1

var a1 = getId();
var a2 = getId();

// a1 -> 1
// a2 -> 2


// Student类

// 变量和this
// 变量只跟它在定义的位置有关系
// this则可以改变

// arr数组 length=100 元素都是function
arr[0]() => 0
arr[9]() => 9

